name: T2 Shards 5-9 (Rows 511-1020)

on:
  workflow_dispatch:
  schedule:
    - cron: '35 11 * * 1-5'

jobs:
  scrape:
    runs-on: ubuntu-22.04

    strategy:
      fail-fast: false
      max-parallel: 5
      matrix:
        shard_index: [5,6,7,8,9]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install selenium beautifulsoup4 gspread webdriver-manager

      - name: Write credentials
        run: |
          echo '${{ secrets.GSPREAD_CREDENTIALS }}' > credentials.json
          echo '${{ secrets.TRADINGVIEW_COOKIES }}' > cookies.json

      - name: Run scraper
        env:
          SHARD_INDEX: ${{ matrix.shard_index }}
          SHARD_SIZE: 102
          CHECKPOINT_FILE: checkpoint_g3_${{ matrix.shard_index }}.txt
        run: python run_scraper.py
